my_dbt_project:
  outputs:
    dev:
      account: ABC-XYZ
      database: DBT_DB
      password: 'XXX'
      role: DBT_ROLE
      schema: DBT_SCHEMA
      threads: 1
      type: snowflake
      user: xxx
      warehouse: DBT_WH
  target: dev
